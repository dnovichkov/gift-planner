services:
  gift-planner:
    build: .
    container_name: gift-planner
    restart: unless-stopped
    ports:
      - "127.0.0.1:3004:80"
    networks:
      - web

networks:
  web:
    external: true
